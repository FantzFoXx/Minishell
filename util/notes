NOTES :
- si PWD n'est pas set, il le reset des qu'on fais un cd
- si PWD n'est pas set, le prompt avec pwd s'affiche quand meme
	DONC le prompt s'affiche grace a la fonction getcwd

TODO :
- gerer les signaux
- reafficher le shell apres un SIGINT

##### ALGO PRINCIPAL MINISHELL #####

loop principal 
{
	afficher prompt
	recuperation commande 
	trim espaces et tab inutiles 
	split commande 
	appel du handle_command
}

handle_command
{
	si -> non commande dans var env PATH
	{
		si -> non dir && non fichier
		{
			si -> non commande builtin
				erreur pas de commande
		}
		sinon si -> dir
			erreur dir
		sinon si -> fichier
		{
			si -> droits exec
				fork -> lancement commande (gerer erreur script shell ?)
			sinon
				erreur pas de droits exec
		}
	}
	sinon
		fork -> lancement commande
}

err a gerer algo principal :

- si chemin de dossier tout seul
- si fichier sans les droits
- si commande inexistante
- toutes les erreurs liees aux builtins
- (???)

erreurs builtins :

CD :
- No such file or directory
- Not a directory
- Permission denied
- 

####################################
